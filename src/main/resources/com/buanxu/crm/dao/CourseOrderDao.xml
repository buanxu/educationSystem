<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.buanxu.crm.dao.CourseOrderDao">

    <select id="findCountByMap" parameterType="Map" resultType="int">
        select count(name) from t_course_order
    </select>

    <select id="finListByMap" parameterType="Map" resultType="CourseOrder">
        select * from t_course_order
    </select>
</mapper>